export const environment = {
  production: true,
  // Set at build time via Railway/Vercel environment variable VITE_API_BASE_URL,
  // or replace with the Railway backend URL before deploying.
  apiBaseUrl: '/api',
};
